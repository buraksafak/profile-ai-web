# Local `npm run dev`. Leave unset/empty for Docker so the browser uses /api.
VITE_API_URL=http://localhost:3000

# Docker / Ubuntu VPS (docker compose)
# WEB_PORT=80
# API_UPSTREAM=http://host.docker.internal:3000
# FRONTEND_API_URL=/profile-ai-api
# FRONTEND_BASE_PATH=/profile-ai-web/
